# RunPod SDK
runpod~=1.7.9

# Model inference (HF Transformers)
# NOTE: torch is installed separately in Dockerfile with CUDA index
transformers>=4.56.2
huggingface-hub>=0.30.2

# Audio I/O
soundfile>=0.13.1
descript-audio-codec>=1.0.0

# Numerical
numpy>=1.24.0
